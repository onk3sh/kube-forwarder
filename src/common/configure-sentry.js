// Sentry integration is temporarily disabled during the modernization
// (Electron/Vue/electron-vite migration). Re-added in the dependency
// modernization stage with @sentry/electron v5+. See modernization plan.
export default function configureSentry() {}
